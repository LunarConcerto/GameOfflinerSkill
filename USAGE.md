# 使用方法

如何把本仓库的 Skill 用在**另一个项目**里，以及每个阶段可以直接对 AI 下达的指令。

---

## 一、把 Skill 接入另一个项目

Skill 的本质是一个目录（入口为 `SKILL.md`）。接入方式三选一：

### 方式 A：项目级注册（推荐，不复制文件）

在目标项目的 `.opencode/opencode.json`（或 `opencode.json`）里注册本仓库路径：

```json
{
  "$schema": "https://opencode.ai/config.json",
  "skills": {
    "paths": ["E:/AI/Skills"]
  }
}
```

- `paths` 指向本仓库根目录即可，会被**递归扫描** `**/SKILL.md`；
- 若把本仓库 clone 到别处，把路径换成实际 clone 路径；
- 保存后**退出并重启 opencode** 生效。

### 方式 B：复制到项目内

把技能目录复制进目标项目，opencode 会自动发现：

```
<目标项目>/.opencode/skills/offline-login-revival/
├── SKILL.md
├── README.md
└── reference/
```

`.opencode/skill/` 或 `.opencode/skills/` 都会被扫描。

### 方式 C：全局注册

写入本机全局配置 `~/.config/opencode/opencode.json`，则所有项目都可用：

```json
{
  "$schema": "https://opencode.ai/config.json",
  "skills": {
    "paths": ["E:/AI/Skills"]
  }
}
```

> 也可放到 `~/.claude/skills/` 或 `~/.agents/skills/` 下，供支持该约定的工具自动加载。

## 二、触发方式

- **自动触发**：当你的任务命中 `SKILL.md` 的 `description`（关键词如"关服复现""离线登录""自制服务端""客户端重定向"等），Agent 会自行加载该技能。
- **显式触发**：直接在指令里点名，例如：

  > 使用 `offline-login-revival` 技能，对 `D:\Games\XXX` 执行 P0 静态侦察。

- **按需查阅**：Agent 加载 `SKILL.md` 后，会按需读取 `reference/playbook.md`（阶段细节）与 `reference/templates.md`（模板）。

## 三、各阶段指令与说明

下面每条指令都可直接复制给 Agent，把 `<...>` 替换为你的实际路径 / 项目名。所有阶段默认**只分析、不改动**，除非指令中明确要求落地代码。

| # | 指令（可复制） | 作用 | 期望产出 | 参考 |
| --- | --- | --- | --- | --- |
| 总 | `使用 offline-login-revival 技能，阅读 SKILL.md，针对 <客户端目录> 给出一份八阶段执行计划，标出每阶段需要我提供的信息。` | 建立全局路线图 | 项目定制的八阶段计划 | `SKILL.md` |
| P0 | `执行 P0 静态侦察：分析 <客户端目录>，回答 5 个关键问题（登录流程/SDK/配置表/协议表/文件完整性/热更方案），产出登录架构图与三类线索清单。不要修改任何文件。` | 静态分析、挖线索 | 登录架构图 + 协议/配置/热更线索清单 | `playbook.md#P0` |
| P1 | `按 P1 为该客户端搭建观测基础：注入框架（挂起启动、初始化前介入、SHA-256 门控）+ 统一日志（引擎日志与热更 VM 日志落文件）+ 错误探针 + 崩溃兜底。先只做观测，不做任何重定向或伪造。` | 让运行态可见 | 能看到每次登录请求/响应/异常 | `playbook.md#P1` |
| P2 | `执行 P2：把登录全链路每一跳建模为「触发 → C2S → S2C → 下一步」，用协议表模板输出，并标出未闭合的未知项（下一步给它们装探针）。` | 状态机建模 | 登录状态机草图 + 未知项清单 | `playbook.md#P2`、`templates.md#6` |
| P3 | `执行 P3 拦截选型：按决策矩阵列出最小改动的拦截点（模块、RVA/签名、门控哈希、失败回退），并搭一个能监听、能记日志的基础服务端。` | 选拦截层 | 拦截点清单 + 可监听服务端 | `playbook.md#P3` |
| P4 | `执行 P4 重定向与绕过：先做寻址重定向到本地，再按需处理 TLS（降级/证书/代理）与 SDK/中台登录伪造，目标是把客户端所有出网流量落到本地。` | 流量到本地 | 重定向日志 + 客户端走出网络门槛 | `playbook.md#P4` |
| P5 | `执行 P5 逐层补回包：从登录链最上游往下，一次只补一个响应，每补一项以「客户端走到下一步」为通过标准；出错时对照客户端与服务端两侧日志。` | 逐跳实现服务端 | 登录握手逐跳跑通 | `playbook.md#P5` |
| P6 | `执行 P6 时序与推送：找出所有「在应答回调里做初始化」的位置，把其依赖数据提前到对应请求应答之前推送，直到稳定进入主界面。` | 满足时序契约 | 主界面无致命错误 | `playbook.md#P6` |
| P7 | `执行 P7 持续优化：按错误驱动逐项清零；为每个功能做「生效验证」（数值变化、重登进度保留）；补一键闭环脚本、版本适配器与回归测试。` | 可维护、可泛化 | 一键脚本 + 适配器 + 测试 | `playbook.md#P7` |

### 常用组合指令

| 指令 | 说明 |
| --- | --- |
| `根据 reference/templates.md，为本项目生成注入器与 hook 骨架，所有 hook 都要版本门控。` | 直接套模板落地代码 |
| `产出本项目的〈协议表〉与〈登录握手逐跳记录表〉，每条标注证据级别。` | 建立可追溯知识库 |
| `为本项目生成〈时序契约记录表〉，列出所有「应答前必须推送」的数据。` | 防止时序错位崩溃 |
| `把本项目差异（RVA、消息字段、SDK 行为）收敛为 ClientAdapter 配置，业务代码不得含版本判断。` | 多版本复用 |
| `把当前进度整理成 README 的「当前进度」表与证据分级记录。` | 沉淀与交接 |

## 四、注意事项

1. **技能给的是方法，不代替具体项目的常量。** 协议号、字段、RVA、SDK 行为都需在目标项目里重新挖掘，仅流程可复用。
2. **先观测、后改动。** 在 P1 观测就绪前，不要执行 P4 的重定向/伪造。
3. **一次只补一个。** P5 阶段切勿批量补响应，否则无法把改动与日志对应起来。
4. **版本门控是硬要求。** 换版本时更新适配器，而不是放宽校验。
5. **技能不会替你遵守法律边界。** 仅用于学习研究，勿用于运营中的游戏或绕过授权。

## 五、最小示例

在目标项目里，一份完整的开局指令可以是这样：

> 使用 `offline-login-revival` 技能。
> 客户端在 `D:\Games\MyGame`，已确认文件完整。
> 第一步先执行 P0 静态侦察并给出登录架构图与线索清单；
> 第二步按 P1 搭建观测工具（只观测，不改行为）。
> 两步都完成后停下来，把结论和下一步计划汇总给我。

之后按 P2 → P7 逐阶段推进，每阶段结束后要求 Agent 输出该阶段的产出物与证据分级，再进入下一阶段。
